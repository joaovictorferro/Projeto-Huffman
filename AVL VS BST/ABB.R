data <- read.table("~/AVL-vs-ABB/amostra.txt", header=T)
options(scipen=5)
maxY <- max(data$ABB)
maxX <- max(data$size)
minX <- min(data$size)
minY <- min(data$ABB)
plot(data$ABB~data$size, col="red", xlab="Size", ylab="Comparisons", xlim=c(minX, maxX), ylim=c(minY, maxY))
title(main = "ABB")
legend("topleft", c("ABB"), col=c("red"), pch=1, inset = c(0.05, 0.05)) 
